---
import Logo from './Logo.astro';
import { site, nav } from '../config/site';
---

<footer class="border-t" style="border-color: var(--hairline); background: var(--surface-2)">
  <div class="mx-auto max-w-5xl px-5 py-16">
    <div class="flex flex-col gap-12 md:flex-row md:justify-between">
      <div class="max-w-xs">
        <Logo height={30} />
        <p class="mt-3 text-sm leading-loose" style="color: var(--ink-soft)">
          {site.tagline}<br />{site.org}
        </p>
        <span class="pill mt-5"><span class="led"></span>{site.funding}</span>
      </div>

      <nav aria-label="頁尾導覽">
        <ul class="grid grid-cols-2 gap-x-12 gap-y-3">
          {
            nav.map((item) => (
              <li>
                <a href={item.href} class="text-sm transition-colors hover:text-[var(--signal)]" style="color: var(--ink-soft)">
                  {item.label}
                </a>
              </li>
            ))
          }
          <li>
            <a href="/news/post/quiz.html" class="text-sm transition-colors hover:text-[var(--signal)]" style="color: var(--ink-soft)">
              AI 小測驗
            </a>
          </li>
        </ul>
      </nav>
    </div>

    <p class="reading mt-14 border-t pt-6 text-xs" style="border-color: var(--rule); color: var(--ink-faint)">
      {site.copyright}
    </p>
  </div>
</footer>
